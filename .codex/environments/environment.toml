# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "firefly-narrative local environment"

[setup]
script = '''
set -euo pipefail

echo "[codex] setup: /Users/jamiecraik/dev/firefly-narrative"
bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm install --frozen-lockfile || run_node_pm install
'''

[[actions]]
name = "Install deps"
icon = "tool"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm install --frozen-lockfile || run_node_pm install
'''

[[actions]]
name = "Typecheck"
icon = "debug"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm run typecheck
'''

[[actions]]
name = "Lint"
icon = "tool"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm run lint
'''

[[actions]]
name = "Build"
icon = "tool"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm run build
'''

[[actions]]
name = "Run tests"
icon = "test"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm test
'''

[[actions]]
name = "Run app"
icon = "run"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools

  if command -v pnpm >/dev/null 2>&1; then
    pnpm "$@"
    return
  fi

  if [ -x /opt/homebrew/bin/pnpm ]; then
    /opt/homebrew/bin/pnpm "$@"
    return
  fi

  if command -v corepack >/dev/null 2>&1; then
    corepack enable >/dev/null 2>&1 || true
    corepack pnpm "$@"
    return
  fi

  echo "[codex] pnpm is not available in this environment"
  return 127
}

run_node_pm run dev
'''
